syntax = "proto3";

option go_package = "pb";
import "deliveroo.proto";

message Config {
    LLACoords home = 1;
    LocatorConfig locators = 2;
}

message LLACoords {
    float latitude = 1;
    float longitude = 2;
    float altitude = 3;
}

message LocatorConfig {
    DeliverooConfig deliveroo = 1;
}
